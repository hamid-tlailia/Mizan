# تتبع عطل تنزيل بطاقة الصورة

## المحاولة الأولى — الإصدار المنشور

في 22 أغسطس 2026 فُتح التطبيق المنشور على النطاق `hadithapp-npm2ceiq.manus.space`، واستُخدم مثال «إنما الأعمال بالنيات» لبدء تحليل ينشئ بطاقة النتيجة. ظل التحليل في حالة التحميل خلال نافذة الفحص الأولى، لذلك لم يظهر زر «تنزيل صورة» بعد ولا يمكن نسبة العطل إلى محرك الرسم أو التنزيل من هذه المحاولة وحدها.

## الخطوة التالية

بعد اكتمال التحليل، سيُفحص سجل الأخطاء ووحدة `useHadithExport` ومسار إنشاء رابط التنزيل، ثم يعاد الاختبار في التطبيق المنشور بعد الإصلاح.

## إعادة إنتاج مؤكدة

أُنشئ سجل اختبار محلي صحيح بالمفتاح `mizan-hadith-history` وفتحت صفحة ملف الحديث المنشورة. عند الضغط على «تنزيل صورة» ظهر التنبيه التالي حرفياً:

> Attempting to parse an unsupported color function "oklch"

إذن لا يتعلق العطل بتنزيل المتصفح أو `Blob`، بل يصل أحد ألوان OKLCH إلى محرك `html2canvas` رغم تعقيم نسخة بطاقة المشاركة. يجب توسيع التعقيم ليشمل خصائص اللون والخلفية المؤثرة كافة في شجرة النسخة المستنسخة قبل التقاطها.

## النتيجة النهائية

بعد توسيع التعقيم وتفعيل `foreignObjectRendering`، أُعيد الاختبار في التطبيق المنشور على بطاقة اختبار محلية. ظهر تنبيه النجاح: «تم تنزيل بطاقة الصورة.» ولم تظهر رسالة OKLCH أو OKLab. هذا يثبت مرور إنشاء اللوحة وتحويلها إلى PNG وبدء التنزيل بنجاح في بيئة الإنتاج.

## بلاغ محتوى الصورة غير مكتمل

أظهر فحص ملف التنزيل الفعلي أن PNG ليس ملفاً فارغاً من البكسلات، لكنه التقط شريطاً قصيراً من البطاقة فقط (`1792×338` بكسل)، وقُصت منه أسطر الدرجة والملخص والتذييل. هذا يفسر ظهوره فارغاً أو غير صالح للمستخدم على بعض الأجهزة. سيُستبدل التقاط DOM بمولد Canvas أصلي يرسم محتوى بطاقة المشاركة كاملاً بأبعاد ثابتة، فلا يعتمد على حسابات ارتفاع `foreignObjectRendering`.

## تحقق مولد Canvas

بعد نشر مولد Canvas وتحديث عامل الخدمة، نُزّل ملف جديد بحجم `1080×826` بكسل. أظهر الفحص المرئي رأس البطاقة، والمتن، والدرجة وتفاصيلها، والخلاصة، والتذييل كاملاً. بذلك لم يعد ملف PNG فارغاً أو مقصوصاً، كما لا يعتمد على محلل CSS أو حزمة التقاط DOM القديمة.

تحقق آلي إضافي في بيئة التطوير شغّل مولد Canvas مباشرة وفحص بيانات البكسلات: أنتج لوحة `1080×826` فيها `390,190` بكسل غير مطابق للون الخلفية، متجاوزاً حد المحتوى الأدنى (`10,000`) بفارق كبير. ثبت ذلك أن اللوحة مرسومة بمحتوى فعلي وليست فارغة.
